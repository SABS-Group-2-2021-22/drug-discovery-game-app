import React from 'react';
import "./../assay.css"
import { connect } from "react-redux"

class AssayRedux extends React.Component {
    constructor(props) {
        super(props)
    }
}

export default connect()(AssayRedux)
